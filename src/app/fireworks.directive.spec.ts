import { FireworksDirective } from './fireworks.directive';

describe('FireworksDirective', () => {
  it('should create an instance', () => {
    const directive = new FireworksDirective();
    expect(directive).toBeTruthy();
  });
});
